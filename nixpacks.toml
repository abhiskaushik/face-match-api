# Fallback if Railway uses Nixpacks instead of Dockerfile
[phases.setup]
aptPkgs = ["libgl1", "libglib2.0-0", "libsm6", "libxrender1", "libxext6", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}"
